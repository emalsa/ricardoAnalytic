services:
  logger.channel.ra_rating:
    parent: logger.channel_base
    arguments: ['ra_rating']
  ra_rating.rating_crawler:
    class: Drupal\ra_rating\RatingCrawler
    arguments: ['@entity_type.manager', '@queue']
