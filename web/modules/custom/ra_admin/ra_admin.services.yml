services:
  logger.channel.ra_admin_scrapedog:
    parent: logger.channel_base
    arguments: ['ra_admin_scrapedog']

  ra_admin.scrapedog:
    class: Drupal\ra_admin\ScrapedogService
    arguments:
      - '@entity_type.manager'
      - '@database'
      - '@logger.channel.ra_admin_scrapedog'
      - '@http_client'
